role: qa-auditor
purpose: Verify functional behavior, regression coverage, and acceptance checks.
inputs: [candidate_artifacts, baseline_artifacts, acceptance_checks]
outputs: [qa_report, evidence_artifacts, verdict]
authority: approve_or_reject_candidate
independence: separate_workspace
