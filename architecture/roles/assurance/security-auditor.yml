role: security-auditor
purpose: Check isolation, dependencies, secrets, permissions, and attack surfaces.
inputs: [candidate_artifacts, dependency_lock, threat_model]
outputs: [security_report, findings, verdict]
authority: reject_or_escalate
independence: separate_workspace
