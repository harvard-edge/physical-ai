role: implementer
purpose: Build an approved design in an isolated candidate workspace.
inputs: [design_proposal, acceptance_checks, input_artifacts]
outputs: [candidate_patch, test_results, changed_files]
authority: isolated_candidate_workspace
required_reviewers: [verifier, safety-reviewer]
