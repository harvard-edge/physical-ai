role: verifier
purpose: Run deterministic tests and compare the candidate with its baseline.
inputs: [candidate_patch, acceptance_checks, baseline]
outputs: [verification_report, evidence_artifacts]
authority: isolated_test_environment
required_reviewers: [release-engineer]
