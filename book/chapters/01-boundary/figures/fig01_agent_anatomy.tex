\documentclass[tikz,border=14pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{tgheros}
\usepackage{sfmath}
\usepackage{amsmath}
\usepackage{fontawesome5}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds,calc}
\usepackage{xcolor}

\renewcommand{\familydefault}{\sfdefault}

% Professional Academic Palette
\definecolor{crimsonaccent}{HTML}{990000}
\definecolor{slateblue}{HTML}{1E3A8A}
\definecolor{darkslate}{HTML}{0F172A}
\definecolor{bodytext}{HTML}{334155}
\definecolor{cardbg}{HTML}{FFFFFF}
\definecolor{subtlebg}{HTML}{F8FAFC}
\definecolor{bordergray}{HTML}{94A3B8}
\definecolor{lightborder}{HTML}{CBD5E1}

\newcommand{\mputag}[1]{\colorbox{slateblue!10}{\scriptsize\bfseries\color{slateblue}\,#1\,}}
\newcommand{\mcutag}[1]{\colorbox{crimsonaccent!10}{\scriptsize\bfseries\color{crimsonaccent}\,#1\,}}

\begin{document}
\begin{tikzpicture}[
  font=\sffamily,
  >=Stealth,
  organ/.style={
    draw=bordergray,
    fill=cardbg,
    rounded corners=0pt,
    line width=0.9pt,
    inner sep=8pt,
    align=center,
    text=darkslate
  }
]

  % Top Banner: Stage 7 Governance
  \node[organ, draw=slateblue!60, fill=subtlebg, text width=7.4in, line width=1.0pt] (s7) {
    {\normalsize\bfseries\color{slateblue}\faIcon{balance-scale}\;\; 7. GOVERNANCE, LINEAGE \& RELEASE GATE}\\[2pt]
    {\footnotesize STPA Hazard Mitigation $\cdot$ Bumpless Human Joystick Override $\cdot$ Defensible Assurance Cases}
  };

  % --- TOP PIPELINE ROW: Ingestion to State ---
  \node[organ, text width=2.22in, below=0.35in of s7.south west, anchor=north west] (s1) {
    {\bfseries\color{slateblue}\faIcon{satellite-dish}\;\; 1. SENSING}\\[2pt]
    {\footnotesize Photons / Voltages to DMA}\\[2pt]
    {\scriptsize\color{bodytext}MIPI CSI-2 $\cdot$ I2C $\cdot$ SPI Bus Priority}
  };

  \node[organ, text width=2.22in, right=0.37in of s1] (s2) {
    {\bfseries\color{slateblue}\faIcon{eye}\;\; 2. PERCEPTION}\\[2pt]
    {\footnotesize Encoders to Spatial Tokens}\\[2pt]
    {\scriptsize\color{bodytext}\mbox{ViT} $\cdot$ \mbox{DINOv2} $\cdot$ 3D Affordances}
  };

  \node[organ, text width=2.22in, right=0.37in of s2] (s3) {
    {\bfseries\color{slateblue}\faIcon{database}\;\; 3. WORLD MODELS}\\[2pt]
    {\footnotesize Latent Belief \& $SE(3)$ Trees}\\[2pt]
    {\scriptsize\color{bodytext}JEPA / RSSM Dynamics $\cdot$ TTL Validity}
  };

  % --- MIDDLE PIPELINE ROW: Untrusted Proposal Engine (MPU) ---
  \node[organ, draw=slateblue, fill=subtlebg, text width=3.51in, below=0.45in of s3.south east, anchor=north east] (s4) {
    \mputag{\faIcon{server}\;\; SYSTEM 2 $\cdot$ LINUX MPU (UNTRUSTED)}\\[3pt]
    {\bfseries\color{slateblue}\faIcon{brain}\;\; 4. SEMANTIC DELIBERATION}\\[2pt]
    {\scriptsize\color{bodytext}VLMs \& Spatial Foundation Models $\cdot$ Goals $\cdot$ Leases ($t_{\text{expire}}$)}
  };

  \node[organ, draw=slateblue, fill=subtlebg, text width=3.51in, left=0.38in of s4] (s5) {
    \mputag{\faIcon{microchip}\;\; SYSTEM 1.5 $\cdot$ LINUX MPU / NPU (UNTRUSTED)}\\[3pt]
    {\bfseries\color{slateblue}\faIcon{network-wired}\;\; 5. TRAJECTORY DECODERS}\\[2pt]
    {\scriptsize\color{bodytext}Diffusion Policies $\cdot$ \mbox{ACT Action Chunking} ($H$-steps) $\cdot$ Jerk Bounds}
  };

  % --- LOWER ROW: Trusted Real-Time Enforcer (MCU) ---
  \node[organ, draw=crimsonaccent, fill=white, line width=1.2pt, text width=7.4in, below=0.72in of s5.south west, anchor=north west] (s6) {
    \mcutag{\faIcon{shield-alt}\;\; SYSTEM 1 $\cdot$ REAL-TIME MCU (BARE-METAL / RTOS $\cdot$ TRUSTED)}\\[3pt]
    {\bfseries\color{crimsonaccent}\faIcon{tachometer-alt}\;\; 6. REAL-TIME REFLEX \& SAFETY ENFORCEMENT}\\[2pt]
    {\scriptsize\color{bodytext}1 kHz Reflex Timing Loop $\cdot$ Control Barrier Functions ($h(\mathbf{x}) \ge 0$) $\cdot$ Stopping Distance $d_{\text{stop}}(v_t) \le d_{\text{clearance}} \cdot$ Veto ($u_t$)}
  };

  % --- PHYSICAL WORLD ROW ---
  \node[organ, draw=crimsonaccent, fill=subtlebg, line width=1.1pt, text width=7.4in, below=0.40in of s6.south west, anchor=north west] (world) {
    {\bfseries\color{crimsonaccent}\faIcon{cogs}\;\; THE PHYSICAL WORLD ($W_t \to W_{t+1}$)}\\[3pt]
    {\scriptsize\color{bodytext}Kinetic Momentum ($p{=}mv$) $\cdot$ Joule Heat Dissipation ($I^2R$) $\cdot$ Matter Mutation $\cdot$ Friction ($\mu$) $\cdot$ Collision Dynamics}
  };

  % Proposal-Permission Privilege Boundary (Red Dashed Line)
  \coordinate (bleft) at ($(s6.north west) + (0, 0.36in)$);
  \coordinate (bright) at ($(s6.north east) + (0, 0.36in)$);
  \draw[dashed, line width=1.2pt, crimsonaccent] (bleft) -- (bright);

  % Position badge clearly on the right half so it never collides with the left vertical arrow
  \node[font=\sffamily\bfseries\scriptsize, fill=white, draw=crimsonaccent, rounded corners=0pt, inner sep=3.5pt, text=crimsonaccent] 
    at ($(bleft)!0.62!(bright)$) 
    {\faIcon{lock}\;\; THE PROPOSAL--PERMISSION PRIVILEGE BOUNDARY (NO DIRECT MOTOR ACCESS)};

  % Clean Un-occluded Feed-forward Data Flow Arrows
  \draw[->, line width=1.0pt, slateblue] (s1.east) -- (s2.west);
  \draw[->, line width=1.0pt, slateblue] (s2.east) -- (s3.west);
  \draw[->, line width=1.0pt, slateblue] (s3.south) -- (s3.south |- s4.north);
  \draw[->, line width=1.0pt, slateblue] (s4.west) -- (s5.east);
  
  % Vertical Dataflow with White Badge Pills
  \draw[->, line width=1.4pt, dashed, crimsonaccent] (s5.south) -- node[pos=0.25, fill=white, draw=crimsonaccent, rounded corners=0pt, inner sep=2.5pt, font=\sffamily\bfseries\scriptsize\color{crimsonaccent}]{\faIcon{paper-plane}\; Expiring Proposal $p_t$} (s5.south |- s6.north);
  \draw[->, line width=1.4pt, crimsonaccent] (s6.south) -- node[pos=0.5, fill=white, draw=crimsonaccent, rounded corners=0pt, inner sep=2.5pt, font=\sffamily\bfseries\scriptsize\color{crimsonaccent}]{\faIcon{check-circle}\; Permitted Action $u_t = \text{permit}(p_t)$} (world.north);

  % Closed-loop Endogenous Feedback Arrow
  \draw[->, line width=1.0pt, crimsonaccent] (world.west) -- ++(-0.45in,0) |- (s1.west)
    node[pos=0.25, above, rotate=90, font=\sffamily\bfseries\scriptsize\color{crimsonaccent}, align=center]{\faIcon{sync-alt}\;\; Endogenous Sensory Shift ($A_t \to W_{t+1} \to O_{t+1}$)};

\end{tikzpicture}
\end{document}
