# Capstone: The Whole-System Defense\chaptersubtitle{Live Fault Injection, Dual-Brain Integration, and Release Defense}

![Systems Organ Focus: The Complete Integrated Agent under Live Cross-Layer Seeded Fault Injection.](figures/fig_pipeline_locator.svg){#fig-locator-ch99 width=100% fig-pos="H"}

*How do you prove that an autonomous physical AI machine will safely survive unannounced hardware crashes, bus contention, and sensory dropouts before granting permission to deploy?*

In academic AI research, system performance is evaluated by mean task completion rates on clean test benchmarks. In commercial deployment, physical machines must survive adversarial reality: memory bus contention, sensor dropouts, network clock drift, operating system kernel panics, and out-of-distribution optical glare. A machine cannot be deemed safe simply because its models are accurate; it must be architecturally resilient when components fail. The Capstone synthesizes the entire 11-artifact engineering dossier into a live, whole-system defense—subjecting the dual-brain platform to unannounced cross-layer fault injection to prove that physical invariants hold and to justify an evidence-backed release verdict.

{{< pagebreak >}}

::: {.callout-autopsy title="The High-Speed Pallet-Transfer Memory Collapse"}
* **System:** Industrial Dual-Arm High-Speed Palletizing Manipulator (Diffusion VLA Policy v2.4 on Monolithic Tegra SoC).
* **Incident:** The manipulator was transferring a $4.2\text{ kg}$ machined tooling fixture at $1.4\text{ m/s}$ when a background log-rotation daemon initiated an unbuffered disk write, saturating the Unified Memory Architecture (UMA) DRAM bus. The diffusion action chunk unroller suffered a $280\text{ ms}$ tail latency stall ($P_{99.9} = 312\text{ ms}$ versus $24\text{ ms}$ nominal). The arm drifted $14\text{ cm}$ off-trajectory, striking a structural support stanchion and causing a catastrophic cycloidal gearbox fracture ($4,200\text{ USD}$ hardware damage and an 18-hour factory shutdown).
* **Telemetry Snapshot:** Nominal $P_{50}$ policy inference was $24\text{ ms}$; at $T+42.180\text{ s}$, available DRAM memory bandwidth plummeted from $34.2\text{ GB/s}$ to $4.1\text{ GB/s}$, freezing the user-space trajectory generator. Because the system lacked an independent, real-time microcontroller enforcing Control Barrier Functions ($h(x) \ge 0$) and dynamic stopping envelopes ($d_{\text{stop}}$), the motor drivers continued pulsing the last latched PWM duty cycles directly into the 3-phase inverter bridge until impact.
* **Root Cause:** Monolithic architectural trust—untrusted learned software running on the Linux host exercised direct electrical authority over motor PWM registers without an independent, hardware-isolated real-time permission supervisor.
* **Architectural Flaw:** The engineering team shipped the system under a "Deploy" verdict based purely on average trajectory tracking error in clean physics simulation, without subjecting the integrated hardware-software loop to unannounced cross-layer fault injection trials.
* **Provenance:** Engineered composite. The mechanism and the failure path are drawn from documented incident classes; the system, telemetry, and costs are constructed for teaching and are not a report of a specific event.

:::


::: {.callout-objective}
By the conclusion of this capstone chapter, you will be able to:

1. **Execute End-to-End Dual-Brain Autonomous Pick-and-Place:** Coordinate the full 7-stage pipeline (MIPI DMA $\to$ DINOv2 tokens $\to$ $SE(3)$ frame graph $\to$ Edge VLM 3D intent $\to$ Diffusion action chunks $\to$ 1 kHz MCU CBF enforcer) with measured sub-millisecond tail determinism.
2. **Survive Unannounced Cross-Layer Fault Injection:** Prove that the Proposal–Permission architecture safely arrests or isolates live clock skews ($15\text{ ms}$ PTP step), memory bus saturation (UMA stress), MPU kernel panics (`kill -9`), sensor occlusions, and out-of-ODD optical glare without breaking physical invariants ($h(x) \ge 0, d_{\text{clearance}} \ge d_{\text{stop}}$).
3. **Execute the 3-Phase Defense Protocol:** Deliver the 8-minute architectural walkthrough of the complete engineering notebook (loop charter through release case), manage the 5-minute live bench telemetry profiling, and complete the 12-minute rapid root-cause diagnosis.
4. **Render an Evidence-Backed Release Verdict:** Justify a definitive **DEPLOY**, **CONDITION**, or **REFUSE** decision before the 4-Expert Red-Team Review Board using Claim-Argument-Evidence (CAE) safety cases and empirical logic analyzer distributions.
:::

::: {.callout-contract}
| Component | Authority and Substrate | Contract Specification |
| :--- | :--- | :--- |
| **What the MPU Proposes (System 2 / 1.5)** | *Untrusted · Stochastic · Linux MPU* | • 3D semantic bounding box intent leases ($t_{\text{expire}}$)<br>• $H=16$ step action spline chunks ($p_t$)<br>• Multimodal state estimates and perception tokens |
| **What the MCU Permits (System 1)** | *Trusted · Deterministic · Real-Time MCU* | • Zero-allocation 1 kHz Control Barrier Invariants ($h(x) \ge 0$)<br>• Dynamic stopping distance evaluation ($d_{\text{stop}} \le d_{\text{clearance}}$)<br>• Heartbeat watchdog lease monitoring ($\tau_{\text{watchdog}} \le 50\text{ ms}$)<br>• Actuator PWM permission: $u_t = \text{permit}(p_t)$ |
| **Failure Escalation** | *MCU Hardware Reflex* | On any lease timeout, barrier breach, or bus stall, trigger immediate Category 1 dynamic deceleration or Category 0 power relay cutoff. |
:::

::: {.callout-note icon=false}
**The Sim-to-Real Delta:**
* **What Simulation Assumes:** The hardware simulator runs synchronously with zero bus contention; OS threads never experience scheduling jitter; clock signals are globally unified without drift; and model failures simply emit a numeric penalty score.
* **What the Bench Measures:** UMA DRAM bus contention from DMA cameras can quadruple neural inference latency ($P_{99.9} > 300\text{ ms}$); Linux kernel panics leave hardware PWM registers pulsing indefinitely; PTP clock jitter induces fictitious velocity spikes; and collisions break titanium gears with irreversible physical consequence.
:::


## Crossing the Threshold: The Adversarial Systems Trial

For eleven chapters, you have built the vital organs of a Physical AI agent brick by brick. You traced the causal boundary separating digital bits from physical momentum (loop charter), measured empirical $P_{99}$ latency tails against world freshness walls (requirements ledger), scheduled multi-rate dataflows (workflow charter), ingested zero-copy camera streams (observation contract), tracked Lie-algebraic $SE(3)$ frame trees (state and timing model), grounded semantic intents with expiring leases (intent schema), unrolled diffusion action chunks with $\mathcal{C}^2$ jerk continuity (planning schema), caged candidate motions inside 1 kHz Control Barrier Functions (enforcement design), budgeted heterogeneous silicon resources (placement ledger), enforced bumpless human takeover (authority design), and constructed evidence-backed safety cases (release case).

Now, those isolated subsystems converge into a single, breathing physical machine.

In academic machine learning, the final milestone is often a leaderboard metric—an offline validation loss, an F1 score, or a cherry-picked video demonstration of a robot executing a task under ideal studio lighting. In **Physical AI Systems**, a happy-path demonstration proves nothing:

$$\text{Engineering Truth} \ne \text{Mean Performance Under Studio Lighting}$$

$$\text{Defensible Release} \equiv \text{Invariant Survival Under Adversarial Chaos } (W_t \longrightarrow W_{t+1})$$

A physical machine is not verified when it succeeds under nominal conditions; it is verified only when it proves mathematically and physically incorruptible under worst-case hardware faults, software crashes, memory bus contention, and environmental perturbations. When a camera cable experiences microphonic noise, when the Linux kernel panics mid-trajectory, or when an overhead strobe light blinds the vision sensor, the physical laws of kinetic momentum ($\mathbf{p} = m\mathbf{v}$) and Joule heat ($I^2R$) do not pause for a software debugger.

The Capstone Defense is the ultimate trial of your agent. You will bring your completed dual-brain machine to the bench, present your versioned **engineering notebook** (loop charter through release case), demonstrate live closed-loop pick-and-place tracking, survive unannounced instructor-seeded fault injections, diagnose root causes under strict time pressure, and defend an accountable **Release Verdict** before an expert review board.


## 1. Anatomy of the Integrated Dual-Brain Agent

The complete Physical AI Agent operates across two distinct physical compute substrates linked by a shared-memory firewall, as illustrated in @fig-dual-brain-integration:

![**The end-to-end dual-brain agent architecture on the Arduino UNO Q platform.** The untrusted Linux MPU (Host Brain) runs perception, world modeling, semantic reasoning, and diffusion action chunking at multi-rate cadences ($0.5\text{--}60\text{ Hz}$). The trusted real-time MCU (Reflex Brain) enforces Control Barrier Invariants ($h(x) \ge 0$), dynamic stopping envelopes ($d_{\text{stop}}$), and watchdog leases at a strict $1000\text{ Hz}$ hard real-time loop over Tightly-Coupled Memory (TCM) shared SRAM.](figures/fig99_dual_brain_integration.svg){#fig-dual-brain-integration width=100%}

### The 7-Stage Sensory-Motor Pipeline

1. **Stage 1: Sensor Ingestion (60 Hz):** MIPI CSI-2 camera frames stream via zero-copy DMA into circular ring buffers in shared memory. Optical quadrature encoders and 6-DoF IMU measurements are sampled with hardware-latched PTP microsecond timestamps.
2. **Stage 2: Spatial Perception & Affordance Tokenization (60 Hz):** Vision backbones (DINOv2 / MobileNet) project raw pixel arrays into compressed 3D spatial affordance tokens and surface normals.
3. **Stage 3: Temporal World Modeling & $SE(3)$ Frame Tree (50 Hz):** The latent state estimator maintains kinematic transforms across end-effector, camera, fixture, and target coordinates ($T_{\text{target}}^{\text{world}} \in SE(3)$), filtering observations through an innovation gating threshold.
4. **Stage 4: Semantic Deliberation & Reasoning (1–2 Hz):** An edge Vision-Language Model (VLM) evaluates high-level instructions (e.g., *"Pick the blue calibration cylinder"*), resolving visual ambiguities and writing a 3D target bounding box into shared SRAM with a strict expiring intent lease ($t_{\text{expire}}$).
5. **Stage 5: Trajectory Generation & Action Chunking (20 Hz):** A diffusion policy or Action Chunking Transformer (ACT) unrolls an $H=16$ step trajectory chunk ($p_t = [q_1, \dots, q_H]$), ensembling overlapping predictions via cubic Hermite splines to guarantee $\mathcal{C}^2$ jerk continuity.
6. **Stage 6: Real-Time Reflex & Safety Enforcement (1000 Hz):** Running on the isolated ARM Cortex-M4 microcontroller in zero-allocation bare metal, the enforcer executes a Quadratic Program (QP) every $1.0\text{ ms}$:
 $$\min_{u_t} \frac{1}{2} \| u_t - p_t \|^2 \quad \text{subject to} \quad \nabla h(x)^T f(x, u_t) \ge -\alpha(h(x))$$
 The MCU continuously calculates dynamic stopping clearance:
 $$d_{\text{stop}}(t) = v(t) \cdot t_{\text{delay}} + \frac{v(t)^2}{2 a_{\text{max}}} \le d_{\text{clearance}}$$

7. **Stage 7: Actuation & Governance:** Permitted control commands ($u_t$) modulate the 3-phase inverter gate drivers to power brushless DC (BLDC) motors, mutating physical world state ($W_t \to W_{t+1}$).


## 2. The Systems Defense Trial: Live Seeded Fault Injection

During the live defense, the candidate system must execute continuous closed-loop pick-and-place tracking while the Red-Team Review Board injects unannounced hardware, software, and environmental faults.

| Fault Category | Injected Benchmark Fault Condition | Physical Symptom on Bench | Mandatory Containment Mechanism |
| :--- | :--- | :--- | :--- |
| **Layer 1: Sensor & Clock** | PTP Clock Skew ($\Delta t = 15\text{ ms}$) or synthetic frame drop. | Robot pauses or exhibits sudden velocity estimate jump. | State estimator trips innovation residual gate; MCU rejects stale proposals and holds position (Cat 2). |
| **Layer 2: Memory & Bus** | UMA Memory Bus Saturation (`stress-ng --vm 4 --vm-bytes 80%`). | Action chunk unroller suffers severe $P_{99}$ latency spike ($> 200\text{ ms}$). | Watchdog lease countdown decays; MCU gracefully decelerates at $a_{\text{max}}$ before stopping boundary (Cat 1). |
| **Layer 3: OS & Runtime** | Linux Kernel Panic / Sudden Process Kill (`kill -9` on Python runtime). | Linux MPU heartbeat GPIO toggle ceases immediately. | Hardware watchdog timer trips in $\tau_{\text{watchdog}} \le 35\text{ ms}$; MCU opens power interlock relays (Cat 0/1). |
| **Layer 4: Perception & Model** | Out-of-ODD Optical Glare ($> 10,000\text{ Lux}$ strobe on camera lens). | VLM hallucinates target pose $+40\text{ cm}$ outside workspace. | Control Barrier Function clips candidate trajectory to workspace geofence hull: $u_t = \text{proj}_{\partial \mathcal{C}}(p_t)$. |
| **Layer 5: Actuator & Thermal** | Motor Phase Over-Current ($I > I_{\text{max}}$) or winding $T > 85^\circ\text{C}$. | High coil heating under stalled mechanical fixture. | MCU shunt current monitor triggers hardware torque derating, preventing permanent coil demagnetization. |

: The Cross-Layer Seeded Fault Taxonomy. Candidate systems must demonstrate 100% containment across all 5 fault layers during the live defense trial. {#tbl-seeded-fault-matrix}

::: {.callout-note title="Cross-Embodiment Anchor: Whole-System Capstone Defense Across the 3 Archetypes"}
* **Archetype 1: Locomotion & Mobility (Autonomous Delivery Drone & Vehicle):** 
 Full-system defense trial: Demonstrating closed-loop waypoint navigation while surviving unannounced GPS jamming, camera lens spray occlusion, and CAN-bus latency spikes. The ASIL-D MCU detects sensor corruption, engages Category 1 dynamic deceleration, and safely flares to an emergency landing without ground impact.

* **Archetype 2: Contact Manipulation (The Desk Bench Kit / Manipulator):** 
 Full-system defense trial on the Arduino UNO Q: Demonstrating autonomous pick-and-place with OpenVLA + ACT while surviving unannounced MPU kernel panics (`kill -9`) and UMA DRAM bus saturation. The Cortex-M4 MCU catches watchdog expiration in $\le 35\text{ ms}$, evaluates Control Barrier Functions, and halts in $d_{\text{stop}} = 5.2\text{ cm} \le d_{\text{clearance}} = 12.0\text{ cm}$.

* **Archetype 3: Cybernetic Energy (Smart Grid Substation Inverters):** 
 Full-system defense trial: Autonomous feeder voltage regulation and battery storage dispatch while surviving unannounced SCADA communication blackouts and transmission line faults. The digital signal controller trips hardware crowbar protection within $<5\,\mu\text{s}$, containing voltage transients within IEEE 1547 standards.
:::

### Dynamic Fault Containment Timeline

@fig-seeded-fault-timeline illustrates the exact millisecond-by-millisecond progression captured on a multi-channel logic analyzer when an unannounced MPU process crash is injected mid-motion:

![**Cross-layer seeded fault injection and dynamic arrest waveform.** At $t_{\text{fault}} = 50\text{ ms}$, the Linux MPU experiences a synthetic kernel crash (`kill -9`), causing the heartbeat GPIO toggle to die. The MCU hardware watchdog countdown timer decays from $50\text{ ms}$ to zero, tripping an unmaskable interrupt at $t = 85\text{ ms}$ ($\tau_{\text{watchdog}} = 35\text{ ms}$). The MCU enforcer immediately overrides the MPU and engages Category 1 dynamic deceleration ($a_{\text{max}} = -4.5\text{ m/s}^2$), bringing the end-effector to a complete physical stop at $t = 185\text{ ms}$ with $d_{\text{stop}} = 5.2\text{ cm} \le d_{\text{clearance}} = 12.0\text{ cm}$. Zero physical contact occurs.](figures/fig99_seeded_fault_timeline.svg){#fig-seeded-fault-timeline width=100%}


## 3. The 11-Artifact engineering notebook Walkthrough

The central deliverable of the Capstone Defense is the complete, versioned **engineering notebook**. Every artifact represents an evidence-backed engineering contract:

| Artifact Code | Artifact Deliverable Title | Load-Bearing Physical Parameter / Contract | Chapter Origin |
| :---: | :--- | :--- | :---: |
| **loop charter** | **Loop Charter** | Physical causality boundary, authority model, and irreversibility scope ($W_t \to W_{t+1}$). | Chapter 1 |
| **requirements ledger** | **Requirements & Latency Ledger** | Environmental deadline ($\tau_{\text{world}}$), empirical $P_{99}$ latency tail, and stopping distance ($d_{\text{stop}}$). | Chapter 2 |
| **runtime skeleton** | **Continuous Runtime Skeleton** | Multi-rate task cadences, lock-free seqlocks, and hardware watchdog timeout bounds ($\tau_{\text{watchdog}}$). | Chapter 3 |
| **observation contract** | **Observation Contract** | MIPI CSI-2 DMA ring buffer layout, PTP timestamping, and sensor failure drop thresholds. | Chapter 4 |
| **state and timing model** | **State, Frames & Timing Model** | $SE(3)$ Lie group frame tree ($T_{\text{tool}}^{\text{world}}$), EKF innovation gates, and temporal belief TTL leases. | Chapter 5 |
| **intent schema** | **Policy Interface & Intent Schema** | 3D bounding box goal representations and expiring semantic intent leases ($t_{\text{expire}}$). | Chapter 6 |
| **planning schema** | **Planning & Trajectory Schema** | Action horizon chunks ($H=16$), cubic Hermite spline blending, and $\mathcal{C}^2$ jerk limits ($j_{\text{max}}$). | Chapter 7 |
| **enforcement design** | **Action Limits & Enforcement Design** | Zero-allocation 1 kHz QP Control Barrier Functions ($h(x) \ge 0$) and ISO 13850 stop escalation. | Chapter 8 |
| **placement ledger** | **Placement Map & Resource Ledger** | Silicon mapping (MPU vs. MCU vs. NPU), SRAM allocations, DRAM bandwidth, and power budgets. | Chapter 9 |
| **authority design** | **Human-Authority & Lineage Record** | Bumpless joystick takeover state machine, truncated episode tagging, and governed OTA lineage. | Chapter 10 |
| **release case** | **Integrated Deployment Case** | Formal ODD matrix, 4-rung qualification evidence, Claim-Argument-Evidence (CAE) safety case. | Chapter 11 |

: The Complete 11-Artifact engineering notebook. The formal engineering deliverables defending system correctness from causality to release. {#tbl-cumulative-dossier}


## 4. The 4-Expert Red-Team Review Board and Oral Defense Protocol

The oral defense is a structured 25-minute engineering examination conducted live around the bench:

| Defense Phase | Time Allocation | Candidate Deliverables & Review Board Focus |
| :--- | :---: | :--- |
| **Phase 1: Dossier Architecture Walkthrough** | **8 Minutes** | • Present 7-stage silicon placement map and multi-rate cadences.<br>• Defend the 11-artifact engineering notebook (loop charter $\to$ release case).<br>• Justify the Claim-Argument-Evidence (CAE) safety case and state the release verdict. |
| **Phase 2: Live Closed-Loop Bench Demonstration** | **5 Minutes** | • Execute continuous autonomous pick-and-place tracking on Arduino UNO Q kit.<br>• Display real-time logic analyzer GPIO waveforms showing $P_{50}/P_{99}$ latency tails.<br>• Demonstrate bumpless manual supervisor takeover and resume. |
| **Phase 3: Live Seeded Fault Trial & Triage** | **12 Minutes** | • Review board injects an unannounced fault from the canonical 5-fault taxonomy.<br>• System must prove 100% containment ($d_{\text{stop}} \le d_{\text{clear}}$, zero crashes).<br>• Candidate team isolates root cause from flight telemetry using `Hypothesis -> Bisect -> Confirm`. |

: The 3-Phase Oral Defense Protocol. Time allocations and evaluation criteria for the live bench examination. {#tbl-defense-phases}


@fig-defense-dossier-matrix illustrates how the cumulative dossier and live defense trials are evaluated across the 4 red-team auditing perspectives to reach an accountable release verdict:

![**The oral defense and release verdict qualification gate.** The 11-artifact engineering notebook is audited across four non-negotiable engineering dimensions: Embedded Silicon determinism, Embodied ML grounding, System Safety invariants, and Practitioner UX reproducibility. The review culminates in a binding Release Verdict: **DEPLOY** (unconstrained in-ODD), **CONDITION** (restricted envelope), or **REFUSE** (shipment blocked).](figures/fig99_defense_dossier_matrix.svg){#fig-defense-dossier-matrix width=100%}

### The 4 Red-Team Auditing Lenses

1. **The Embedded Silicon & Real-Time Lead:** Audits zero-allocation memory guarantees on the MCU ($0\text{ bytes dynamic heap}$), UMA bus contention budgets, hardware watchdog countdown limits ($\le 50\text{ ms}$), and microsecond PTP timestamp synchronization.
2. **The Modern Embodied ML Specialist:** Audits 3D spatial grounding of goal tokens, latency amortization across action chunks ($H=16$), $\mathcal{C}^2$ jerk continuity during temporal ensembling, and model abstention behavior under optical glare.
3. **The Robotics & System Safety Lead:** Audits dynamic stopping distance equations ($d_{\text{stop}} \le d_{\text{clearance}}$), ISO 13850 fallback escalation correctness, truncated episode tagging for behavioral cloning flywheels, and CAE safety claim validity.
4. **The Student & Practitioner UX Lead:** Audits zero-magic bench reproducibility, diagnostic clarity under pressure, and systematic execution of the 3-step triage loop:
 $$\text{Diagnostic Loop:} \quad \text{Hypothesis} \longrightarrow \text{Bisect} \longrightarrow \text{Confirm}$$


## 5. Industrial Qualification Criteria and The Release Verdict

To earn an industrial release qualification, the candidate system must satisfy four non-negotiable passing criteria:

1. **Independent Permission Invariance:** The team proves through physical schematic and firmware inspection that the real-time MCU safety enforcer cannot be bypassed, preempted, or disabled by any software failure, deadlock, or exception on the Linux MPU.
2. **Quantitative Tail Metrology:** All timing and safety claims are backed by empirical latency distributions ($P_{99.9}$) captured via hardware-triggered logic analyzer GPIO pins, rather than idealized average benchmarks.
3. **100% Seeded Fault Containment:** The system safely executes controlled physical deceleration ($a_{\text{max}}$) without physical collision under 100% of injected faults.
4. **Defensible Release Case:** The team presents an honest, evidence-backed release verdict:
 * **DEPLOY:** All verification standards met; unconstrained autonomous release permitted inside the specified Operational Design Domain (ODD).
 * **CONDITION:** Release permitted only under restricted operating conditions (e.g., maximum speed derated to $50\%$, payload limited to $0.25\text{ kg}$, or mandatory human supervisor in visual line of sight).
 * **REFUSE:** The system failed fault containment or violated latency tail budgets; field deployment is physically blocked until architectural redesign.


## Systems Fallacies and Pitfalls

::: {.callout-warning}
### Common Engineering Fallacies in Capstone Defenses
* **Fallacy 1: The "Happy-Path 100-Cycle" Fallacy.** *"Our robot completed 100 consecutive pick-and-place cycles in the lab without a single mistake, so our system is proven safe for production."* 
 *Correction:* 100 successful runs only test the center of the nominal data distribution ($P_{50}$). Physical safety is governed entirely by the extreme tail ($P_{99.99}$)—what happens when a camera cable drops a frame, a memory bus saturates, or a lighting reflection blinds the sensor during run 101.
* **Fallacy 2: The "Application-Level Software Guardrail" Fallacy.** *"We wrote an assertion in our Python controller script that checks if the arm is near an obstacle and stops the trajectory."* 
 *Correction:* An assertion running inside user-space Python on Linux shares the same operating system kernel, garbage collector, and memory bus as the policy. If the process hangs, the assertion never executes. Safety guards must run on an independent, hardware-isolated microcontroller.
* **Fallacy 3: The "Cherry-Picked Video Demo" Fallacy.** *"We recorded a polished video of the robot performing the task and included high-level architectural block diagrams in our presentation."* 
 *Correction:* A video demonstrates visual plausibility, not engineering defensibility. An industrial defense requires inspectable logic analyzer waveforms, hardware flight logs, and rigorous mathematical proofs of invariant containment.
:::


::: {.callout-decision}
### Notebook checkpoint: Master engineering notebook Sign-Off (capstone defense package / release case)
Freeze the complete, bound engineering notebook (loop charter through release case), incorporating live bench fault injection telemetry, empirical logic analyzer tail distributions, and the final signed Release Verdict:

```yaml
# =============================================================================
# CUMULATIVE DESIGN DOSSIER: MASTER CAPSTONE RELEASE ARTIFACT (release case)
# =============================================================================
system_identity:
 system_name: "Physical-AI-DualBrain-Agent"
 hardware_platform: "Arduino UNO Q Reference Kit (Linux MPU + Cortex-M4 MCU)"
 firmware_version: "v1.0.0-release"
 dossier_hash: "0x7F4A9B2C3D8E1F5A"

cumulative_dossier_manifest:
 loop_01_charter: { status: "AUDITED", boundary: "Proposal-Permission Split" }
 req_01_requirements: { status: "AUDITED", p99_tail_ms: 48.2, d_stop_margin_cm: 6.8 }
 run_01_runtime: { status: "AUDITED", mcu_loop_hz: 1000.0, watchdog_timeout_ms: 50.0 }
 obs_01_observation: { status: "AUDITED", dma_zero_copy: true, ptp_sync_us: 12.4 }
 state_01_frames: { status: "AUDITED", se3_tree_valid: true, belief_ttl_ms: 100.0 }
 intent_01_intent: { status: "AUDITED", 3d_bounding_box: true, lease_ttl_ms: 500.0 }
 plan_01_planning: { status: "AUDITED", horizon_steps: 16, c2_jerk_continuity: true }
 enf_01_enforcement: { status: "AUDITED", cbf_invariants: true, dynamic_stopping: true }
 place_01_placement: { status: "AUDITED", mpu_watts: 4.8, mcu_sram_dynamic_bytes: 0 }
 auth_01_governance: { status: "AUDITED", bumpless_override: true, lineage_tracked: true }
 rel_01_assurance: { status: "AUDITED", cae_safety_case: "VALIDATED" }

live_defense_trial_results:
 nominal_cycles_completed: 50
 nominal_cycle_success_rate: 1.000
 seeded_fault_injection_campaign:
 layer_1_ptp_clock_skew: { injected: 5, contained: 5, pass_rate: 1.00 }
 layer_2_uma_bus_choke: { injected: 5, contained: 5, pass_rate: 1.00 }
 layer_3_mpu_kernel_panic: { injected: 5, contained: 5, pass_rate: 1.00 }
 layer_4_optical_glare: { injected: 5, contained: 5, pass_rate: 1.00 }
 layer_5_human_override: { injected: 5, contained: 5, pass_rate: 1.00 }
 total_fault_containment_rate: 1.000 # 25/25 unannounced faults safely arrested
 mean_fault_diagnosis_duration_min: 6.4 # Limit: 12.0 minutes

operational_design_domain:
 max_linear_velocity_mps: 0.50
 max_payload_mass_kg: 0.50
 ambient_illumination_lux_range: [150, 2500]
 ambient_temperature_c_range: [10.0, 42.0]
 workspace_geofence_box_m: [[-0.35, 0.35], [0.10, 0.60], [0.00, 0.50]]

defense_release_verdict: "DEPLOY"

expert_review_board_signoff:
 embedded_silicon_lead: { verdict: "PASS", signature: "A. M. Garavagno" }
 embodied_ml_lead: { verdict: "PASS", signature: "V. Janapa Reddi" }
 system_safety_lead: { verdict: "PASS", signature: "Chief Safety Architect" }
 practitioner_ux_lead: { verdict: "PASS", signature: "MSc Lead Auditor" }
```
:::


::: {.callout-lab}
### Hands-on Realization: Lab 99 — The Final Capstone Systems Defense
Mount your fully integrated **Physical AI Kit (Arduino UNO Q Platform)** to the test bench, connect multi-channel logic analyzer probes to the MPU heartbeat, MCU enforcer, and motor current shunt lines, and initiate continuous autonomous pick-and-place tracking. Deliver your 8-minute dossier presentation to the review board, survive the unannounced instructor-seeded fault injection, diagnose the root cause within 12 minutes using the `Hypothesis -> Bisect -> Confirm` protocol, and defend your final signed **Release Verdict** (release case).
:::
