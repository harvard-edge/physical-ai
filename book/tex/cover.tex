% ===========================================================================
% Physical AI Systems, cover and title pages
% ===========================================================================
%
% Loaded after tex/preamble.tex, which defines the palette this file uses.
% This file draws; the preamble configures. Keep it that way.
%
% \maketitle produces two pages:
%
%   1. The cover. Full-bleed ETH blue, crimson rules top and bottom, and the
%      proposal-permission loop drawn as the book's signature diagram. The
%      loop is on the cover because it is the argument: an untrusted proposer,
%      a trusted permitter, an irreversible world, and the feedback that makes
%      the whole thing endogenous.
%   2. The title page. Quiet, typographic, no artwork.
%
% Text on the cover is set here rather than pulled from \@title so the cover
% can be composed as a piece of design. If the book is retitled, change it in
% both places.
%
% ===========================================================================

\makeatletter
\renewcommand*{\maketitle}{%

% --- Page 1: cover ---------------------------------------------------------
\begin{titlepage}\thispagestyle{empty}%
  \newgeometry{margin=0pt}%
  \begin{tikzpicture}[remember picture,overlay]

    % Ground.
    \fill[fill=ethdarkblue!95!black]
      (current page.south west) rectangle (current page.north east);

    % Crimson rule, top.
    \fill[harvardcrimson]
      ([yshift=-0.50in]current page.north west) rectangle
      ([yshift=-0.58in]current page.north east);

    % Series label.
    \node[anchor=north west, text width=6.5in, align=left]
      at ([xshift=1.0in, yshift=-1.05in]current page.north west)
      {\sffamily\bfseries\fontsize{10}{12}\selectfont\color{white!80}%
       \MakeUppercase{A Systems Engineering Textbook}};

    % Title block. The extra 0.16em keeps "Physical AI" from reading as one
    % word at 44pt, where the interword space is proportionally small.
    \node[anchor=north west, text width=6.5in, align=left]
      at ([xshift=1.0in, yshift=-1.6in]current page.north west)
      {\sffamily\bfseries\fontsize{44}{48}\selectfont\color{white}%
       Physical\hspace{0.16em} AI\par
       \vspace{0.2in}%
       {\sffamily\bfseries\fontsize{16}{22}\selectfont\color{white!95}%
        Machine Learning Systems That Sense and Act}\par
       \vspace{0.14in}%
       {\sffamily\fontsize{10.5}{14.5}\selectfont\color{white!80}%
        What must the system know, measure, enforce, and prove before software
        moves matter?}\par
      };

    % The proposal-permission loop.
    \node[anchor=center] at ([yshift=-0.15in]current page.center) {%
      \begin{tikzpicture}[
        scale=0.92,
        stage/.style={
          draw=white!40, fill=ethdarkblue!60!black, text=white,
          rounded corners=3pt, inner sep=7pt, align=center,
          font=\sffamily\small, text width=3.85in},
        proposes/.style={stage, draw=ethbronze,
          fill=ethbronze!20!ethdarkblue, line width=1.2pt},
        permits/.style={stage, draw=ethpetrol,
          fill=ethpetrol!25!ethdarkblue, line width=1.2pt}
      ]
        \node[proposes] (mpu) {%
          \textbf{\color{ethbronze!90!white}LINUX MPU · Untrusted Proposal Engine}\\[1pt]
          {\footnotesize Foundation models $\cdot$ spatial tokens $\cdot$ expiring leases ($p_t$)}};

        \node[permits, below=0.6in of mpu] (mcu) {%
          \textbf{\color{ethpetrol!70!white}REAL-TIME MCU · Trusted Permission Authority}\\[1pt]
          {\footnotesize 1 kHz reflex loop $\cdot$ stopping distance $d_{\text{stop}}$ $\cdot$ hardware veto ($u_t$)}};

        \node[stage, below=0.6in of mcu] (world) {%
          \textbf{PHYSICAL WORLD ($W_t \longrightarrow W_{t+1}$)}\\[1pt]
          {\footnotesize Kinetic momentum $\cdot$ matter mutation $\cdot$ endogenous sensory shift}};

        \draw[ethbronze!90!white, dashed, line width=1.4pt, -{Stealth[length=3mm]}]
          (mpu) -- node[right=4pt, font=\sffamily\bfseries\scriptsize\color{ethbronze!90!white}]
          {Expiring proposal $p_t$} (mcu);

        \draw[ethpetrol!80!white, line width=1.6pt, -{Stealth[length=3mm]}]
          (mcu) -- node[right=4pt, font=\sffamily\bfseries\scriptsize\color{ethpetrol!80!white}]
          {Permitted action $u_t = \text{permit}(p_t)$} (world);

        \draw[white!75, line width=1.0pt, -{Stealth[length=2.5mm]}]
          (world.west) to[out=180,in=180]
          node[left=6pt, font=\sffamily\scriptsize\color{white!80}]
          {Endogenous feedback $O_{t+1}$} (mpu.west);
      \end{tikzpicture}};

    % Author block.
    \node[anchor=south west, text width=6.5in, align=left]
      at ([xshift=1.0in, yshift=0.85in]current page.south west)
      {{\sffamily\bfseries\fontsize{16}{19}\selectfont\color{white}Vijay Janapa Reddi}\par
       \vspace{3pt}%
       {\sffamily\fontsize{11}{14}\selectfont\color{white!90}%
        \textbf{\textcolor{harvardcrimson!90!white}{Harvard University}}%
        \ $\cdot$\ %
        \textbf{\textcolor{ethblue!80!white}{ETH Zurich}}}\par
       \vspace{6pt}%
       {\sffamily\fontsize{8.5}{11}\selectfont\color{white!70}%
        Open curriculum $\cdot$ Arduino dual-brain kit $\cdot$ cumulative design dossier}\par
      };

    % Crimson rule, bottom.
    \fill[harvardcrimson]
      ([yshift=0.35in]current page.south west) rectangle
      ([yshift=0.40in]current page.south east);

  \end{tikzpicture}%
  \restoregeometry
\end{titlepage}%

% --- Page 2: title page ----------------------------------------------------
\begin{titlepage}\thispagestyle{empty}%
  \raggedright
  \vspace*{0.9in}
  {\textcolor{harvardcrimson}{\rule{2.8in}{1.6pt}}\par}
  \vspace{0.35in}
  {\sffamily\bfseries\fontsize{28}{32}\selectfont\color{structural}\@title\par}
  \vspace{0.12in}
  {\sffamily\bfseries\fontsize{13}{17}\selectfont\color{consequence}\@subtitle\par}
  \vspace{0.8in}
  {\sffamily\large\bfseries\color{structural}\@author\par}
  \vspace{0.06in}
  {\sffamily\normalsize\color{softink}Harvard University \ $\cdot$\  ETH Zurich\par}
  \vspace{0.25in}
  {\sffamily\small\color{softink}\@date\par}
  \vfill
  {\sffamily\bfseries\color{consequence}$W_t \longrightarrow W_{t+1}$\par}
  \vspace{0.15in}
  {\sffamily\footnotesize\color{paleink}%
   Learned proposals, deterministic permission, and defensible physical release.\par}
  \vspace{0.4in}
\end{titlepage}%

}
\makeatother
