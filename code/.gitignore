# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# robot speech cache (regenerated on demand)
body/assets/cache/

# the LIVE memory graph grows at runtime; the tracked artifact is
# memory/graph.example.jsonl. Commit a snapshot deliberately if you want history.
memory/graph.jsonl

.DS_Store
