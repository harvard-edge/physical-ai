# voice

Turning text into speech.

- On the robot: Piper, local and offline, so the robot never needs the cloud
  just to speak.
- On a dev Mac: the built in `say` command, pre rendered to 16 kHz mono WAV.

Cached clips live here. Phase 2 for the on-robot Piper path.
